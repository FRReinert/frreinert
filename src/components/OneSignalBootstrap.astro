---
/**
 * OneSignal Web SDK v16 — Custom Code setup.
 * @see https://documentation.onesignal.com/docs/en/web-sdk-setup
 */
import { onesignalAppId, PUSH } from '../lib/push';

const appId = onesignalAppId();
const { serviceWorkerPath, serviceWorkerScope } = PUSH;
---

<script
  is:inline
  src="https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.page.js"
  defer
/>
<script is:inline define:vars={{ appId, serviceWorkerPath, serviceWorkerScope }}>
  window.OneSignalDeferred = window.OneSignalDeferred || [];
  OneSignalDeferred.push(async function (OneSignal) {
    await OneSignal.init({
      appId,
      notifyButton: { enable: false },
      serviceWorkerPath,
      serviceWorkerParam: { scope: serviceWorkerScope },
      allowLocalhostAsSecureOrigin:
        location.hostname === 'localhost' || location.hostname === '127.0.0.1',
    });
    document.documentElement.dataset.onesignal = 'ready';
    window.dispatchEvent(new CustomEvent('onesignal:ready'));
  });
</script>
